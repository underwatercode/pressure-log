import { Screen } from '../components/Screen';

export function Home() {
  return (
    <Screen title="Home">
      <p className="screen-note">
        Today's reading, quick-log buttons, and the marshmallow mascot land here in a later
        milestone.
      </p>
    </Screen>
  );
}
