import { Screen } from '../components/Screen';

export function Log() {
  return (
    <Screen title="Log">
      <p className="screen-note">
        The unified timeline of readings, events, meals, sleep, and water lands here in a later
        milestone.
      </p>
    </Screen>
  );
}
